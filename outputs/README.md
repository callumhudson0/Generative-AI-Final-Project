# outputs/

This folder is where generated files are saved when the notebooks are run, including:

- `bexley_monthly_hotspots.csv` — the engineered LSOA-month hotspot dataset
- `bexley_hotspot_random_forest.joblib` — the saved tuned Random Forest model

These files are not tracked by git (see `.gitignore`). Run the notebooks to regenerate them.
